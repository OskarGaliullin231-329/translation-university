#include <fstream>
#include <iostream>

#include "include/preprocessor.h"

int main() {
    std::ifstream src_file;
    std::string src_contents;
    std::string line;
    while (getline(src_file, src_contents)) {
        src_contents
    }
    return 0;
}
