#include <stdio.h>

// entry point of the program
int main() {
    /* 
    let's declare some variables
    */
    int a, b, result;

    result = a + b; // and let's see summation result
    return 0;
}
